package r.nodes;


public enum BinaryOperator {
    ASSIGN,
    SUPER_ASSIGN,

    ADD,
    SUB,
    MULT,
    DIV,
    MOD,

    POW,

    MODEL,
    COLUMN,

    GE, GT,
    LE, LT,
    EQ, NE,

    OR,
    BITWISEOR,
    AND,
    BITWISEAND,

    WHILE,
    IF
}
