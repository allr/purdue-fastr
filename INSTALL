# Installation

In a fresh directory (for instance `mkdir fastr ; cd fastr`):

 - `hg clone https://maxinevm@lafo.ssw.uni-linz.ac.at/hg/graalvm-truffle-codegen truffle`
 - `hg clone http://r.cs.purdue/hg/fastr`
 - `export MX_PATH=../fastr/mx `
 - `cd truffle ; ./mx.sh ideinit`
     + Eclipse: Switch to your new workspace, Import \ existing projects

# Run
